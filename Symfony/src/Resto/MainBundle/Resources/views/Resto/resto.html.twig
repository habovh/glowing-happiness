{% extends "RestoMainBundle::mainLayout.html.twig" %}

{% block contenu %}

<!-- RESTO -->
<div class="hero-unit">
	<h1>{{ resto.nom }}</h1>
	<hr>
	<p>{{ resto.description }}</p>
</div>

<div class="row-fluid">
	<div class="span12">

		
		{% for unplat in plats %}
		<div class="span3 plats">
			<div class="thumbnail plat">
				<img data-src="holder.js/150x150" alt="{{ unplat.nom }}" style="width: 150px; height: 150px;" src="{{ asset('images/plats/originales' ~ unplat.image) }}">
				 <div class="caption">
				 	<h3>{{ unplat.nom }}</h3>
				 	<p>{{ unplat.description }}</p>
				 	<p>
				 		<a class="btn btn-primary" href="#">Ajouter au panier</a>
				 		<a class="btn" href="">{{ unplat.prix }} €</a>
				 	</p>
				 </div>
			</div>
		</div>
		{% endfor %}


	</div>
</div>

{% endblock %}